```prisma
// Ajout du modèle BannedUser
model BannedUser {
  id       String   @id @default(uuid())
  email    String   @unique
  phone    String?
  reason   String
  bannedAt DateTime @default(now())
}
```